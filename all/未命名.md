```mermaid
erDiagram
    hotel_evaluation {
        bigint id PK "ID"
        bigint room_id "房间ID"
        bigint order_id "订单ID"
        decimal(2,1) room_grade "房间评价"
        decimal(2,1) service_grade "服务评价"
        varchar(512) words "评语"
        datetime create_time "创建时间"
        varchar(255) create_by "创建人"
        datetime update_time "修改时间"
        varchar(255) update_by "修改人"
        varchar(255) img_path "图片地址"
    }

    hotel_order {
        bigint id PK "ID"
        bigint room_id "房间ID"
        char(10) status "状态"
        datetime start_time "入住时间"
        datetime end_time "结束时间"
        datetime create_time "创建时间"
        varchar(255) create_by "创建人"
        datetime update_time "修改时间"
        varchar(255) update_by "修改人"
        varchar(255) remarks "备注"
    }

    hotel_resident {
        bigint id PK "ID"
        varchar(255) name "姓名"
        varchar(255) card_type "证件类型"
        varchar(255) cardNumber "证件号码"
        varchar(255) sex "性别"
        varchar(255) nation "民族"
        date birthday "出生日期"
        bigint city "省市县"
        varchar(255) address "详址"
        varchar(255) card_img "证件照片"
        bigint dept_id "录入酒店"
        datetime create_time "录入时间"
        varchar(255) create_by "录入人"
        varchar(11) phone_number "手机号码"
    }

    hotel_room {
        bigint room_id PK "房间ID"
        bigint floor "楼层"
        varchar(30) room_number "房间号"
        char(4) room_type "房间类型"
        bigint hotel_id "所属酒店ID"
        char(1) status "房间状态"
        varchar(64) create_by "创建者"
        datetime create_time "创建时间"
        varchar(64) update_by "更新者"
        datetime update_time "更新时间"
        varchar(255) room_img "房间照片"
        varchar(255) title "展示标题"
        decimal(10,2) price "价格"
        int area "面积"
        varchar(255) hotel_name "酒店名称"
    }

    order_resident_mid {
        bigint resident_id PK "住户ID"
        bigint order_id "订单ID"
    }

    sys_city {
        bigint city_id PK "城市编码"
        bigint pid "父级ID"
        varchar(255) ancestors "递归父级ID"
        varchar(100) city_name "城市名称"
        char(1) city_type "类型"
        char(1) status "状态"
        int city_num "显示顺序"
        varchar(64) for_short "拼音简称"
        datetime create_time "创建时间"
        varchar(64) create_by "创建者"
        datetime update_time "修改时间"
    }

    sys_config {
        int config_id PK "参数主键"
        varchar(100) config_name "参数名称"
        varchar(100) config_key "参数键名"
        varchar(500) config_value "参数键值"
        char(1) config_type "系统内置"
        varchar(64) create_by "创建者"
        datetime create_time "创建时间"
        varchar(64) update_by "更新者"
        datetime update_time "更新时间"
        varchar(500) remark "备注"
    }

    sys_dept {
        bigint dept_id PK "部门ID"
        bigint parent_id "父部门ID"
        varchar(50) ancestors "祖级列表"
        varchar(30) dept_name "部门名称"
        int order_num "显示顺序"
        varchar(20) leader "负责人"
        varchar(11) phone "联系电话"
        varchar(50) email "邮箱"
        char(1) status "部门状态"
        char(1) del_flag "删除标志"
        varchar(64) create_by "创建者"
        datetime create_time "创建时间"
        varchar(64) update_by "更新者"
        datetime update_time "更新时间"
    }

    sys_dict_data {
        bigint dict_code PK "字典编码"
        int dict_sort "字典排序"
        varchar(100) dict_label "字典标签"
        varchar(100) dict_value "字典键值"
        varchar(100) dict_type "字典类型"
        varchar(100) css_class "样式属性"
        varchar(100) list_class "表格回显样式"
        char(1) is_default "是否默认"
        char(1) status "状态"
        varchar(64) create_by "创建者"
        datetime create_time "创建时间"
        varchar(64) update_by "更新者"
        datetime update_time "更新时间"
        varchar(500) remark "备注"
    }

    sys_dict_type {
        bigint dict_id PK "字典主键"
        varchar(100) dict_name "字典名称"
        varchar(100) dict_type "字典类型"
        char(1) status "状态"
        varchar(64) create_by "创建者"
        datetime create_time "创建时间"
        varchar(64) update_by "更新者"
        datetime update_time "更新时间"
        varchar(500) remark "备注"
    }

    sys_job {
        bigint job_id PK "任务ID"
        varchar(64) job_name "任务名称"
        varchar(64) job_group "任务组名"
        varchar(500) invoke_target "调用目标字符串"
        varchar(255) cron_expression "cron表达式"
        varchar(20) misfire_policy "错误策略"
        char(1) concurrent "是否并发"
        char(1) status "状态"
        varchar(64) create_by "创建者"
        datetime create_time "创建时间"
        varchar(64) update_by "更新者"
        datetime update_time "更新时间"
        varchar(500) remark "备注"
    }

    sys_job_log {
        bigint job_log_id PK "任务日志ID"
        varchar(64) job_name "任务名称"
        varchar(64) job_group "任务组名"
        varchar(500) invoke_target "调用目标字符串"
        varchar(500) job_message "日志信息"
        char(1) status "执行状态"
        varchar(2000) exception_info "异常信息"
        datetime create_time "创建时间"
    }

    sys_logininfor {
        bigint info_id PK "访问ID"
        varchar(50) user_name "用户账号"
        varchar(128) ipaddr "登录IP"
        varchar(255) login_location "登录地点"
        varchar(50) browser "浏览器类型"
        varchar(50) os "操作系统"
        char(1) status "登录状态"
        varchar(255) msg "提示消息"
        datetime login_time "访问时间"
    }

    sys_menu {
        bigint menu_id PK "菜单ID"
        varchar(50) menu_name "菜单名称"
        bigint parent_id "父菜单ID"
        int order_num "显示顺序"
        varchar(200) path "路由地址"
        varchar(255) component "组件路径"
        varchar(255) query "路由参数"
        int is_frame "是否为外链"
        int is_cache "是否缓存"
        char(1) menu_type "菜单类型"
        char(1) visible "菜单状态"
        char(1) status "状态"
        varchar(100) perms "权限标识"
        varchar(100) icon "菜单图标"
        varchar(64) create_by "创建者"
        datetime create_time "创建时间"
        varchar(64) update_by "更新者"
        datetime update_time "更新时间"
        varchar(500) remark "备注"
    }

    sys_notice {
        int notice_id PK "公告ID"
        varchar(50) notice_title "公告标题"
        char(1) notice_type "公告类型"
        longblob notice_content "公告内容"
        char(1) status "公告状态"
        varchar(64) create_by "创建者"
        datetime create_time "创建时间"
        varchar(64) update_by "更新者"
        datetime update_time "更新时间"
        varchar(255) remark "备注"
    }

    sys_oper_log {
        bigint oper_id PK "日志主键"
        varchar(50) title "模块标题"
        int business_type "业务类型"
        varchar(100) method "方法名称"
        varchar(10) request_method "请求方式"
        int operator_type "操作类别"
        varchar(50) oper_name "操作人员"
        varchar(50) dept_name "部门名称"
        varchar(255) oper_url "请求URL"
        varchar(128) oper_ip "主机地址"
        varchar(255) oper_location "操作地点"
        varchar(2000) oper_param "请求参数"
        varchar(2000) json_result "返回参数"
        int status "操作状态"
        varchar(2000) error_msg "错误消息"
        datetime oper_time "操作时间"
    }

    sys_post {
        bigint post
```